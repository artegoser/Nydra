{
	"name": "glorichess",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "NODE_ENV=development pnpm wasm:build && NODE_ENV=development vite dev",
		"build": "NODE_ENV=production pnpm wasm:build && NODE_ENV=production vite build",
		"preview": "vite preview",
		"check": "NODE_ENV=development pnpm wasm:build && NODE_ENV=development svelte-kit sync && NODE_ENV=development svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "NODE_ENV=development pnpm wasm:build && NODE_ENV=development svelte-kit sync && NODE_ENV=development svelte-check --tsconfig ./tsconfig.json --watch",
		"format": "prettier --write .",
		"lint": "prettier --check . && eslint .",
		"db:push": "drizzle-kit push",
		"db:migrate": "drizzle-kit migrate",
		"db:studio": "drizzle-kit studio",
		"rust:fmt": "cargo fmt --all --check",
		"rust:lint": "cargo clippy --workspace --all-targets --all-features",
		"rust:test": "cargo test --workspace",
		"rust:check": "cargo check --workspace",
		"wasm:build": "wasm-pack build crates/glorichess-wasm --target web --out-dir ../../src/lib/wasm/pkg --out-name glorichess",
		"test": "cargo test --workspace && pnpm check",
		"test:full": "cargo test --workspace && cargo test --release -p glorichess-chess perft::tests:: -- --ignored && cargo check --workspace && cargo clippy --workspace --all-targets --all-features && pnpm check && NODE_ENV=production vite build",
		"browserslist:update": "pnpm dlx update-browserslist-db latest"
	},
	"devDependencies": {
		"@eslint/compat": "^1.2.3",
		"@sveltejs/adapter-static": "^3.0.10",
		"@sveltejs/kit": "^2.9.0",
		"@sveltejs/vite-plugin-svelte": "^5.0.0",
		"autoprefixer": "^10.4.20",
		"drizzle-kit": "^0.22.0",
		"eslint": "^9.7.0",
		"eslint-config-prettier": "^9.1.0",
		"eslint-plugin-svelte": "^2.36.0",
		"globals": "^15.0.0",
		"prettier": "^3.3.2",
		"prettier-plugin-svelte": "^3.2.6",
		"prettier-plugin-tailwindcss": "^0.6.5",
		"svelte": "^5.0.0",
		"svelte-check": "^4.0.0",
		"tailwindcss": "^3.4.9",
		"typescript": "^5.0.0",
		"typescript-eslint": "^8.0.0",
		"vite": "^6.0.0"
	},
	"dependencies": {
		"@libsql/client": "^0.9.0",
		"@tailwindcss/container-queries": "^0.1.1",
		"@tailwindcss/forms": "^0.5.9",
		"@tailwindcss/typography": "^0.5.15",
		"drizzle-orm": "^0.33.0"
	}
}
